# npm configuration.
#
# legacy-peer-deps is REQUIRED, not a convenience: the React 19 dependency graph has
# transitive peers (notably in the wallet stack) that still declare React 18, and npm's
# strict peer resolution refuses to install them. `npm ci` and `npm install` both inherit
# this flag, so a clean clone installs correctly with no extra arguments.
legacy-peer-deps=true
