# Keeps the empty notes directory in git.
# Do NOT put a .md file here as documentation — the glob in src/lib/notesIndex.js matches *.md
# and would publish it as a note. Publishing instructions live in CLAUDE.md.
