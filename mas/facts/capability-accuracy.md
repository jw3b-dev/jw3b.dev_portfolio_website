# Capability Accuracy — grounding jw3b's pillars in AgileGypsy's real language

**Why this exists:** John directed (twice) that **agilegypsy.com is the source of truth**
for how his work is described. The MAS requirements draft invented a metaphor ("The
Swarm / neural mesh") for the AI pillar — **"the swarm doesn't exist."** This doc replaces
invented framing with the real, evidence-backed language from `agilegypsy-website`
(`app/(marketing)/ai/page.tsx`, `lib/work/projects.ts`, `app/(marketing)/labs/page.tsx`).

> **CORRECTION LOG:** ❌ "The Swarm / Overmind — neural mesh" → ✅ AI = *deterministic
> multi-agent orchestration + GraphRAG + edge RAG*, under the thesis *"AI that reasons
> over structure — systems are graphs."* "Overmind" is real (the platform); "Swarm" is not.

## The AI thesis (verbatim)
- **"Intelligent systems, built on graphs."**
- **"AI that reasons over structure, not just text."**
- **"Systems are graphs. Model them, then prove it."**
- *"an LLM over flat text guesses; an LLM over a graph reasons about structure — who
  connects to whom, what depends on what, which path an attacker or a whale can take."*
- Discipline / voice: **"proof, not promises"** — every shipped claim is clickable to its evidence; roadmap items are labelled "in development," not shipped.

## The four SHIPPED AI capabilities (real names + real proof)
| Capability | Verbatim description | Proof (studio evidence) | jw3b projects it maps to |
|---|---|---|---|
| **Agentic workflows** | "Deterministic multi-agent orchestration — poll → execute → validate → unblock, with a zero-trust validator between every step." | MB-Agentic governance engine (Durable-Object Director; audited against itself, dormant routes "published as found"); Art of Zeta seven-agent DO fleet over 27 strategy daemons, risk+ledger separate authorities, paper-trade by default | MB-agentic, Art of Zeta |
| **GraphRAG on Neo4j** | "Retrieval that beats flat vector search — hybrid vector + full-text over a knowledge graph, grounded and cited." | EcoGraph (knowledge graph of the Neo4j corpus; measured hybrid-retrieval lift) | (studio) — jw3b analog: the `/audit` RAG precedent retrieval |
| **Graph-native tokenomics** | "Token economies modelled as graphs and fused with Monte-Carlo simulation — the graph⇄sim bridge." | Neo4j + GDS + radCAD on real token models (DevGuild $GUILD) | DevGuild, Art of Zeta |
| **RAG at the edge** | "Grounded retrieval on Cloudflare Workers AI + Vectorize, with MCP tool bridges so agents act on real systems safely." | Production across Kointel, KTHULHU + the on-site Guide | Kointel, KTHULHU, jw3b's own concierge/`/audit` RAG |

- **Platform:** **Overmind GenAI Engine** — *"the platform beneath the products: 13 ordered
  phases, durable state, verification as a first-class step."* **Overmind Engine v2** = the
  roadmap scope (Neo4j agent memory, AI-agent GraphRAG, Agent-ID & Web3 agent security,
  **graph-native security-audit engine** on a Code Property Graph AST+CFG+DFG).
- **KTHULHU = "the integration test"** — runs most capabilities at once against a real workload.

## Implication for the four pillars (the graph through-line)
The graph is not just the AI pillar — it is the **connective spine** across all four:
- **Web3 Data** — on-chain activity as a graph (who connects to whom, fund flows).
- **Security** — Code Property Graph auditing (the KTHULHU engine direction); attack-path reasoning.
- **AI** — GraphRAG, agentic orchestration, graph agent memory.
- **Tokenomics** — **graph-native**: token economies modelled as graphs + Monte-Carlo (DevGuild).
Plus John's Neo4j certification and the graphify tooling he already uses → the thesis is *earned*, not decorative.

## CLAIMS DISCIPLINE (critical)
The numbers on agilegypsy.com/ai trace to the **studio's** register (`docs/venture-inventory.md`),
NOT jw3b's. For jw3b.dev, **carry the FRAMING and capability language, but every NUMBER must
trace to `docs/PORTFOLIO_REFERENCE.md` or be dropped** (per the approved claims decision:
drop the unsourced ones). Do not import studio metrics (4,866 lines, 99.76%, 9,828 entities,
+18pts, EcoGraph figures) into jw3b unless they are in jw3b's own register.

## New projects surfaced (not in the current jw3b PROJECTS list)
- **DevGuild ($GUILD)** — graph-native tokenomics case (Neo4j+GDS+radCAD).
- **EcoGraph** — GraphRAG over the Neo4j corpus.
- **Overmind GenAI Engine** — the platform layer beneath KTHULHU/Kointel/Art of Zeta.
> `[REQUIRES JOHN]` decide whether any of these appear on jw3b.dev (they may be studio-only).
